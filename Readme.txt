El sistema usa sqlite3 como base de datos, en la carpeta app encontrar el archivo init_db.py 
que generara el archivo database.db con el usuario admin necesario para el ingreso al sistema,
por otro lado la contraseña del mismo es admin.

